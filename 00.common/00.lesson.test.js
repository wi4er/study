describe("Lesson 1", () => {

    describe("Identifier", () => {
        test("Should create identifier", () => {
            let a; // Lexical Binding identifier

            const b = 0; // Binding with Initializer
        });

        test("Should create binding list", () => {
            let a, b, c; // Binding list
        });
    });

    test("Should create variable", () => {
        let a;
        let b;
        let someLongName;
    });

    // test("Should be ")
    test("Should create value be literal", () => {
        10; // Number literal
        "qwerty"; // String literal;
        [1, 2, 3]; // Array literal
    });

    test("Should create value by operator", () => {
        !true; // Unary Operator
        10 > 20; // Binary operator
        true ? 123 : 321; // Ternary operator
    });

    test("Should", () => {

    });
});
